除了使用synchronized处理线程安全问题，还可以使用jdk5.0提供的Lock锁的方式

synchronized同步的方式与Lock的对比：
    》synchronized不管是同步代码块还是同步方法，都需要在一对{}之后才释放对同步监视器的调用
    》Lock是通过两个方法控制需要被同步的代码，更灵活一些
    》Lock作为接口，提供了多种实现类，适合更多更复杂的场景，效率更高